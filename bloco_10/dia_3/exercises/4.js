const maiusculo = (string) => string.toUpperCase();
 
const primeiraLetra = (string) => string[0];

const juntarStrings = (a, b) => a.concat(b);

module.exports = { maiusculo, primeiraLetra, juntarStrings }